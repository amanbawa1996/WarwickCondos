// /**
//  * Express server setup for authentication API
//  * This file should be imported and used in your main server entry point
//  */

// import express, { Express } from 'express';
// import cookieParser from 'cookie-parser';
// import authRoutes from './routes';

// export function setupAuthServer(app: Express): void {
//   // Middleware
//   app.use(express.json());
//   app.use(cookieParser());

//   // Auth routes
//   app.use('/', authRoutes);

//   console.log('✅ Auth server configured');
// }

// export default setupAuthServer;
